"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _create = require("../create");

var _DoNotConstruct = _interopRequireDefault(require("./DoNotConstruct"));

// Copyright 2017-2020 @polkadot/types authors & contributors
// SPDX-License-Identifier: Apache-2.0
describe('DoNotConstruct', () => {
  const registry = new _create.TypeRegistry();
  it('does not allow construction', () => {
    expect(() => new (_DoNotConstruct.default.with())(registry)).toThrow(/Cannot construct unknown type DoNotConstruct/);
  });
  it('does not allow construction (with Name)', () => {
    expect(() => new (_DoNotConstruct.default.with('Something'))(registry)).toThrow(/Cannot construct unknown type Something/);
  });
});