"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _create = require("../create");

var _Null = _interopRequireDefault(require("./Null"));

// Copyright 2017-2020 @polkadot/types authors & contributors
// SPDX-License-Identifier: Apache-2.0
describe('Null', () => {
  const registry = new _create.TypeRegistry();
  it('compares against null', () => {
    expect(new _Null.default(registry).eq(null)).toBe(true);
  });
  it('compares against Null', () => {
    expect(new _Null.default(registry).eq(new _Null.default(registry))).toBe(true);
  });
  it('compares against other (failed)', () => {
    expect(new _Null.default(registry).eq()).toBe(false);
  });
  it('has no hash', () => {
    expect(() => new _Null.default(registry).hash).toThrow();
  });
  it('isEmpty', () => {
    expect(new _Null.default(registry).isEmpty).toBe(true);
  });
  it('has an empty hex', () => {
    expect(new _Null.default(registry).toHex()).toEqual('0x');
  });
  it('has a Null type', () => {
    expect(new _Null.default(registry).toRawType()).toEqual('Null');
  });
});